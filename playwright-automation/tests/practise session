/*function add(a,b){
    return a+b;
}

let sum=add;
console.log(sum(2,3));
*/
x = findMax(1, 123, 500, 115, 44, 88);
console.log(x);


function findMax() {
  let max = 0;
  for (let i = 0; i < arguments.length; i++) {
    if (arguments[i] > max) {
      max = arguments[i];
    }
  }
  return max;
}

